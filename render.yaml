services:
  - type: web
    name: speed-radar-server
    runtime: python
    repo: https://github.com/NullName404/speed-radar-server
    plan: free
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
